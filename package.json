{
	"name": "DumpsOnDemand",
	"private": true,
	"scripts": {
		"test": "npm -s run lint",
		"lint": "eslint --ext .json . && npm -s run lint:i18n",
		"lint:i18n": "banana-checker i18n/"
	},
	"devDependencies": {
		"eslint-config-wikimedia": "0.29.1",
		"grunt-banana-checker": "0.13.0"
	}
}
